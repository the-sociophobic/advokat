
import { ifIn } from 'advokat/helpers/if-in';
import { module, test } from 'qunit';

module('Unit | Helper | if in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifIn([42]);
  assert.ok(result);
});

