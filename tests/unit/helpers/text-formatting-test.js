
import { textFormatting } from 'advokat/helpers/text-formatting';
import { module, test } from 'qunit';

module('Unit | Helper | text formatting');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textFormatting([42]);
  assert.ok(result);
});

