
import { metroLogo } from 'advokat/helpers/metro-logo';
import { module, test } from 'qunit';

module('Unit | Helper | metro logo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = metroLogo([42]);
  assert.ok(result);
});

