<div class='page {{if noScroll 'no-scroll'}}'>
<div class="container">
  <div style="margin: 0 0 15px 0; overflow-y: auto">

    <div style="width: 50%; float: left; padding-right: 12px">
      <div style="height: 70px">
        <p class="text-20 text-center" style='margin-bottom: 0'>{{{rental-category-rooms model.category model.rooms}}} {{#if (eq model.category 'квартира')}} квартира{{/if}}</p>
        <p class="text-26 text-center" style='margin-bottom: 10px'>{{model.address}}</p>
      </div>
      {{image-view images=model.images noScrollToggle=(action 'noScrollToggle')}}
    </div>
    <div style="width: calc(50% - 15px); float: left; margin-left: 15px; padding: 0 0 10px 10px">
      <div style="height: 70px; padding: 25px 50px 0 30px">
        {{#link-to 'rentals' (query-params type=model.type category=model.property_type)}}Назад к списку объектов{{/link-to}}
        <a style='cursor: pointer; float: right' onClick="window.print()">Распечатать</a>
      </div>

      <div class='blockk' style='width: 100%; overflow-y: auto; padding: 15px 50px 15px 30px'>
        <span class='text-20'>{{number-spaces model.price}} рублей{{#if (eq model.type 'арендовать')}}/месяц{{/if}}</span>
        {{#if (eq model.type 'купить')}}
          <a style='cursor: pointer; float: right' {{action 'toCalc'}}>{{number-spaces monthly}} руб./мес.</a><br>
          <small class='text-grey'>{{{price-per-meter model.price model.area}}} руб./м²</small>
        {{/if}}
      </div>
      <br>

      {{model.district}} район<br>
      <div style='padding-left: 20px'>{{{metro-logo model.metro}}}</div><br>

      <div style='width: 100%; overflow-y: auto; margin: 20px 0'>
        <div class='text-grey' style='width: calc(55% - 10px); padding-left: 30px; float: left'>
          {{#unless  (eq model.property_type 'загородная')}}
            Общая площадь:<br>
            {{#unless (eq model.property_type 'коммерческая')}}
              Жилая площадь:<br>
              Площадь комнат:<br>
              Площадь кухни:<br>
            {{/unless}}
            Этаж/Этажей:
            {{#if (eq model.type 'купить')}}
              <br>Тип дома:
              <br>Вид сделки:
            {{/if}}
          {{else}}
            Площадь дома:<br>
            Площадь участка:<br>
          {{/unless}}
        </div>
        <div style='width: calc(45% - 10px); float: left'>
          {{dot-to-comma model.area}} м²<br>
          {{#unless  (eq model.property_type 'загородная')}}
            {{#unless (eq model.property_type 'коммерческая')}}
              {{dot-to-comma model.living_space}} м²<br>
              {{dot-to-comma model.room_space}} м²<br>
              {{dot-to-comma model.kitchen_space}} м²<br>
            {{/unless}}
            {{model.floor}}/{{model.floors_total}}
          {{else}}
            {{dot-to-comma model.lot_area}} м²
          {{/unless}}
          {{#if (eq model.type 'купить')}}
            <br>{{model.building_type}}
            {{#unless (eq model.property_type 'новостройки')}}
              <br>{{model.deal_status}}
            {{/unless}}
          {{/if}}
        </div>
      </div>

      <div style='width: 100%; overflow-y: auto; margin: 20px 0'>
        {{{text-formatting model.description}}}
      </div>

      <div style='width: 100%; overflow-y: auto; overflow-x: visible; margin-top: 20px; padding: 0 5px 20px 5px'>
        <button class="left" style='width: calc(50% - 10px); height: 35px; margin-right: 20px' {{action 'popup1'}}>Записаться на просмотр</button>
        <button class="left" style='width: calc(50% - 10px); height: 35px' {{action 'popup2'}}>Задать вопрос</button>
      </div>

      <div class='blockk' style='width: 100%; overflow-y: auto; padding: 15px 15px 15px 100px'>
        <div style='width: 20%;float: left'>
          агент:<br>
          телефон:<br>
          {{#if model.agent.mail}}
            почта:<br>
          {{/if}}
        </div>
        <div style='width: 80%; float: left'>
          {{model.agent.name}} {{model.agent.surname}}<br>
          {{phone-formatting model.agent.phone}}<br>
          {{#if model.agent.mail}}
            {{model.agent.mail}}<br>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
  {{location-map location=model.address}}
  {{#if (eq model.type 'купить')}}
    <p class='text-20 text-center'>калькулятор ипотеки</p>
    {{mortgage-calc total=model.price monthly=monthly}}
  {{/if}}
</div>

{{callback-form shown=showPopup1 title='Записаться на просмотр' description='оставьте заявку, мы обязательно с Вами свяжемся'}}
{{callback-form shown=showPopup2 title='Задать вопрос' description='задайте вопрос, мы обязательно Вам ответим'}}
</div>