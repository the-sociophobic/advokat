
import { priceShortMillion } from 'advokat/helpers/price-short-million';
import { module, test } from 'qunit';

module('Unit | Helper | price short million');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceShortMillion([42]);
  assert.ok(result);
});

