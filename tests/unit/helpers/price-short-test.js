
import { priceShort } from 'advokat/helpers/price-short';
import { module, test } from 'qunit';

module('Unit | Helper | price short');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceShort([42]);
  assert.ok(result);
});

