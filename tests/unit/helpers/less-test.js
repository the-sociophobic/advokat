
import { less } from 'advokat/helpers/less';
import { module, test } from 'qunit';

module('Unit | Helper | less');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = less([42]);
  assert.ok(result);
});

