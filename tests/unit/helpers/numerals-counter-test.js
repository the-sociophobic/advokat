
import { numeralsCounter } from 'advokat/helpers/numerals-counter';
import { module, test } from 'qunit';

module('Unit | Helper | numerals counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numeralsCounter([42]);
  assert.ok(result);
});

