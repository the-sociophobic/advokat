
import { pricePerMeter } from 'advokat/helpers/price-per-meter';
import { module, test } from 'qunit';

module('Unit | Helper | price per meter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pricePerMeter([42]);
  assert.ok(result);
});

