
import { dotToComma } from 'advokat/helpers/dot-to-comma';
import { module, test } from 'qunit';

module('Unit | Helper | dot to comma');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dotToComma([42]);
  assert.ok(result);
});

