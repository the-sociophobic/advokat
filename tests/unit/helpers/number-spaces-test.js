
import { numberSpaces } from 'advokat/helpers/number-spaces';
import { module, test } from 'qunit';

module('Unit | Helper | number spaces');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberSpaces([42]);
  assert.ok(result);
});

