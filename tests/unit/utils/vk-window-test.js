import vkWindow from 'advokat/utils/vk-window';
import { module, test } from 'qunit';

module('Unit | Utility | vk window');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vkWindow();
  assert.ok(result);
});
