
import { priceShortCalm } from 'advokat/helpers/price-short-calm';
import { module, test } from 'qunit';

module('Unit | Helper | price short calm');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceShortCalm([42]);
  assert.ok(result);
});

