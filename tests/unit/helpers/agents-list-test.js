
import { agentsList } from 'advokat/helpers/agents-list';
import { module, test } from 'qunit';

module('Unit | Helper | agents list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = agentsList([42]);
  assert.ok(result);
});

