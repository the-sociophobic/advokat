
import { priceShortThousand } from 'advokat/helpers/price-short-thousand';
import { module, test } from 'qunit';

module('Unit | Helper | price short thousand');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceShortThousand([42]);
  assert.ok(result);
});

