import yandexMap from 'advokat/utils/yandex-map';
import { module, test } from 'qunit';

module('Unit | Utility | yandex map');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = yandexMap();
  assert.ok(result);
});
