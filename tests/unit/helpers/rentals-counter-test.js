
import { rentalsCounter } from 'advokat/helpers/rentals-counter';
import { module, test } from 'qunit';

module('Unit | Helper | rentals counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalsCounter([42]);
  assert.ok(result);
});

