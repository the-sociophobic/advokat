
import { daNet } from 'advokat/helpers/da-net';
import { module, test } from 'qunit';

module('Unit | Helper | da net');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = daNet([42]);
  assert.ok(result);
});

