
import { addition } from 'advokat/helpers/addition';
import { module, test } from 'qunit';

module('Unit | Helper | addition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addition([42]);
  assert.ok(result);
});

