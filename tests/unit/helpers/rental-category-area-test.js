
import { rentalCategoryArea } from 'advokat/helpers/rental-category-area';
import { module, test } from 'qunit';

module('Unit | Helper | rental category area');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalCategoryArea([42]);
  assert.ok(result);
});

