
import { spbAddress } from 'advokat/helpers/spb-address';
import { module, test } from 'qunit';

module('Unit | Helper | spb address');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spbAddress([42]);
  assert.ok(result);
});

