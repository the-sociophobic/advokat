
import { imageViewOffset } from 'advokat/helpers/image-view-offset';
import { module, test } from 'qunit';

module('Unit | Helper | image view offset');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageViewOffset([42]);
  assert.ok(result);
});

