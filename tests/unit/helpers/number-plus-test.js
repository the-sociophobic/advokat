
import { numberPlus } from 'advokat/helpers/number-plus';
import { module, test } from 'qunit';

module('Unit | Helper | number plus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberPlus([42]);
  assert.ok(result);
});

